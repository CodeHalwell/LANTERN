# lantern/models/__init__.py — Model API

This initializer consolidates the core architectural primitives—recursive transformer blocks and sparse attention—highlighting the theoretical focus on **depth-sharing** and **context-efficient** attention as the building blocks for LANTERN.
